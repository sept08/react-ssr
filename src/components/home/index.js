import React from 'react';

const Home = () => {
    return (
        <div>
            <div>home</div>
            <button onClick={() => alert('home')}>click</button>
        </div>)
};

export default Home;
